{
  "name": "runhouse-inventory-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker holding the Shopify Admin credential for the Run House Inventory Manager. Stage 1: read-only GET /catalog.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "parity": "node test/parity.mjs",
    "test:auth": "node test/auth.mjs",
    "test:needham": "node test/needham.mjs",
    "test:known-set": "node test/known-set.mjs",
    "test:apparel": "node test/apparel-catalog.mjs",
    "test": "node test/auth.mjs && node test/needham.mjs && node test/known-set.mjs && node test/apparel-catalog.mjs && node test/parity.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.61.1"
  }
}
